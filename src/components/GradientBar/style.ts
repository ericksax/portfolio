import styled from "@emotion/styled";

export const Gradient = styled.div`
  height: 24px;
  width: 100%;
  position: sticky;
  top: 0;
  z-index: 2;
  background: linear-gradient(90deg, #0f037e, #4a47f0, #00a6ff, #00f7ff);
`;
